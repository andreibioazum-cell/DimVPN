name: Build GodBlock Animated

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - run: sudo apt-get update && sudo apt-get install imagemagick -y

      - name: Setup Structure
        run: |
          mkdir -p src/main/java/me/godblock
          mkdir -p src/main/resources/assets/godblock/{blockstates,models/block,models/item,textures/block}
          mkdir -p src/main/resources/data/godblock/recipes

      # Текстура
      - name: Generate Texture
        run: |
          convert -size 16x16 xc:purple god.png
          mv god.png src/main/resources/assets/godblock/textures/block/god_block.png

      - name: Blockstate & Models
        run: |
          cat << 'EOF' > src/main/resources/assets/godblock/blockstates/god_block.json
          { "variants": { "": { "model": "godblock:block/god_block" } } }
          EOF

          cat << 'EOF' > src/main/resources/assets/godblock/models/block/god_block.json
          { "parent": "block/cube_all", "textures": { "all": "godblock:block/god_block" } }
          EOF

          cat << 'EOF' > src/main/resources/assets/godblock/models/item/god_block.json
          { "parent": "godblock:block/god_block" }
          EOF

      # ✅ Крафт: уголь в центре, вокруг камень
      - name: Recipe
        run: |
          cat << 'EOF' > src/main/resources/data/godblock/recipes/god_block.json
          {
            "type": "minecraft:crafting_shaped",
            "pattern": [
              "SSS",
              "SCS",
              "SSS"
            ],
            "key": {
              "S": { "item": "minecraft:stone" },
              "C": { "item": "minecraft:coal" }
            },
            "result": {
              "item": "godblock:god_block",
              "count": 1
            }
          }
          EOF

      - name: build.gradle
        run: |
          cat << 'EOF' > build.gradle
          plugins { id 'fabric-loom' version '1.5-SNAPSHOT' }

          group = 'me.godblock'
          version = '2.0'

          repositories {
              mavenCentral()
              maven { url 'https://maven.fabricmc.net/' }
          }

          dependencies {
              minecraft "com.mojang:minecraft:1.20.1"
              mappings "net.fabricmc:yarn:1.20.1+build.10:v2"
              modImplementation "net.fabricmc:fabric-loader:0.15.11"
              modImplementation "net.fabricmc.fabric-api:fabric-api:0.92.1+1.20.1"
          }

          java { toolchain.languageVersion = JavaLanguageVersion.of(17) }
          EOF

      - run: echo "rootProject.name = 'GodBlock'" > settings.gradle

      - name: fabric.mod.json
        run: |
          cat << 'EOF' > src/main/resources/fabric.mod.json
          {
            "schemaVersion": 1,
            "id": "godblock",
            "version": "2.0",
            "name": "God Block",
            "entrypoints": {
              "main": ["me.godblock.GodBlockMod"]
            },
            "depends": {
              "fabricloader": ">=0.15.0",
              "minecraft": "1.20.1",
              "fabric-api": "*"
            }
          }
          EOF

      # ✅ Новый код с NBT
      - name: GodBlock Code
        run: |
          cat << 'EOF' > src/main/java/me/godblock/GodBlockMod.java
          package me.godblock;

          import net.fabricmc.api.ModInitializer;
          import net.fabricmc.fabric.api.object.builder.v1.block.entity.FabricBlockEntityTypeBuilder;
          import net.minecraft.block.*;
          import net.minecraft.block.entity.*;
          import net.minecraft.entity.player.PlayerEntity;
          import net.minecraft.item.*;
          import net.minecraft.nbt.NbtCompound;
          import net.minecraft.registry.*;
          import net.minecraft.server.world.ServerWorld;
          import net.minecraft.sound.SoundCategory;
          import net.minecraft.sound.SoundEvents;
          import net.minecraft.text.Text;
          import net.minecraft.util.*;
          import net.minecraft.util.hit.BlockHitResult;
          import net.minecraft.util.math.BlockPos;
          import net.minecraft.world.World;

          public class GodBlockMod implements ModInitializer {

              public static Block GOD_BLOCK;
              public static BlockEntityType<GodBlockEntity> GOD_BLOCK_ENTITY;

              public void onInitialize() {

                  GOD_BLOCK = Registry.register(Registries.BLOCK,
                          new Identifier("godblock","god_block"),
                          new Block(AbstractBlock.Settings.copy(Blocks.NETHERITE_BLOCK).strength(5f)) {

                              @Override
                              public ActionResult onUse(BlockState state, World world, BlockPos pos,
                                                        PlayerEntity player, Hand hand, BlockHitResult hit) {

                                  if (!world.isClient && player.isSneaking()) {

                                      BlockEntity be = world.getBlockEntity(pos);
                                      if (be instanceof GodBlockEntity gbe) {

                                          gbe.mode++;
                                          if (gbe.mode > 3) gbe.mode = 1;
                                          gbe.markDirty();

                                          player.sendMessage(Text.literal("Mode: " + gbe.mode), false);

                                          world.playSound(null, pos,
                                                  SoundEvents.BLOCK_BEACON_POWER_SELECT,
                                                  SoundCategory.BLOCKS, 1f, 1f);
                                      }
                                  }
                                  return ActionResult.SUCCESS;
                              }

                              @Override
                              public BlockEntity createBlockEntity(BlockPos pos, BlockState state) {
                                  return new GodBlockEntity(pos, state);
                              }

                              @Override
                              public boolean hasBlockEntity(BlockState state) {
                                  return true;
                              }
                          });

                  Registry.register(Registries.ITEM,
                          new Identifier("godblock","god_block"),
                          new BlockItem(GOD_BLOCK,new Item.Settings()));

                  GOD_BLOCK_ENTITY = Registry.register(
                          Registries.BLOCK_ENTITY_TYPE,
                          new Identifier("godblock","god_block_entity"),
                          FabricBlockEntityTypeBuilder.create(GodBlockEntity::new, GOD_BLOCK).build()
                  );
              }

              public static class GodBlockEntity extends BlockEntity {

                  public int mode = 1;

                  public GodBlockEntity(BlockPos pos, BlockState state) {
                      super(GOD_BLOCK_ENTITY, pos, state);
                  }

                  @Override
                  public void writeNbt(NbtCompound nbt) {
                      super.writeNbt(nbt);
                      nbt.putInt("Mode", mode);
                  }

                  @Override
                  public void readNbt(NbtCompound nbt) {
                      super.readNbt(nbt);
                      mode = nbt.getInt("Mode");
                  }
              }
          }
          EOF

      - run: gradle wrapper
      - run: ./gradlew build

      - uses: actions/upload-artifact@v4
        with:
          name: GodBlockNBT
          path: build/libs/*.jar
